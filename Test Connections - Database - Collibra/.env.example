# Collibra API Client Configuration
# Copy this file to .env and fill in your actual credentials

# ============================================================================
# Required: OAuth 2.0 Credentials (for REST API v2.0)
# ============================================================================
# Get these from Collibra: Settings > OAuth Applications
# Create a new OAuth application if you don't have one yet
COLLIBRA_BASE_URL=https://your-instance.collibra.com
COLLIBRA_CLIENT_ID=your_oauth_client_id
COLLIBRA_CLIENT_SECRET=your_oauth_client_secret

# ============================================================================
# Optional: Basic Auth Credentials (for Catalog Database Registration API)
# ============================================================================
# These are only needed if you want to use Basic Auth instead of OAuth Bearer token
# for the Catalog Database Registration API. By default, the client uses OAuth Bearer token.
# If not provided, the DatabaseConnectionManager will use OAuth Bearer token automatically.
COLLIBRA_BASIC_AUTH_USERNAME=your_collibra_username
COLLIBRA_BASIC_AUTH_PASSWORD=your_collibra_password

# ============================================================================
# Optional: Request Timeout
# ============================================================================
# Default timeout is 30 seconds. Uncomment and adjust if needed:
# COLLIBRA_TIMEOUT=30
